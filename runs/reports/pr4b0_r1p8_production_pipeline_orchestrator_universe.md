# PR4B0-R1P8 Production Pipeline, Orchestrator, and Universe Evidence

## Mandatory declarations

No real candidate configuration was executed.

No real DEVELOPMENT outcome was produced.

No real VALIDATION outcome was produced.

No real FINAL_HOLDOUT row was accessed.

No protocol was registered for a real research epoch.

No paper evaluation was started.

## Verdict

`PR4B0_R1P8_PRODUCTION_PIPELINE_ORCHESTRATOR_AND_UNIVERSE_COMPLETE`

All three production gaps are repaired. The accepted structural preflight returned exactly `READY_TO_REGISTER_PROTOCOL`; PR4B0-R1 may restart through the production orchestrator from a new isolated epoch root. P8 did not restart it.

## Exact source basis and results

| Repository | Accepted start | Resulting semantic commit | Result |
|---|---|---|---|
| Engine | `76b31415791cc98308bc3f0d4d23bec326cedd0b` | `6075e42c31df456c9faefdae03f1f4cd9695672f` | Production partition path, governed epoch command, universe separation, and tests |
| RIF | `ed2cbc903e5b34052f4f9d63ad99d02101effb37` | `23be9a8ef9b754af4fe61eacea3650404707b484` | Additive universe binding and Engine configuration-byte preservation |
| Historian | `14e0b569cc41e3517d2393d87d39c00559ac408e` | unchanged | Tree remains `74182d50c8f6dc6639c68cd6af9a82656640701c` |

The blocked Engine commit `b9c1e60a845367aee5f33b5c9f52d5a794588ef2` is a direct reports-only child of the accepted P7 commit and contains exactly the blocked JSON/Markdown pair. It was used only as evidence.

## Production partition pipeline

The new `ak.engine.partition_artifact_plan.v1` contract separates checkpoint identity, structural plan, materialized artifact, durable access receipt, and candidate result. It binds exact accepted source-chain identities, both immutable source-root classes, receipt-to-fragment membership, availability evidence, universe, interval, ordering, cache/symlink policies, and canonical plan hash.

The adapter resolved independently sorted checkpoint and daily-manifest hash/path sets by content identity. The accepted checkpoint spans R1P5R gzip backfill fragments and P4 plain canonical prospective fragments. Planning verifies manifests, receipts, identities, paths, and file types without reading fragment bytes. Materialization is available only after exact RIF authorization and rejects alternate roots, symlinks, unregistered caches, changed identities, cross-partition rows, pre-2026 rows, and wrong symbols.

| Partition | Interval | Days | Daily memberships | Plan SHA-256 |
|---|---|---:|---:|---|
| DEVELOPMENT | `[2026-01-01, 2026-04-10)` | 99 | 891 | `5724a094236ce839e1d8c63a9c13c52697f7377bef0752982347e6516aff4ac8` |
| VALIDATION | `[2026-04-10, 2026-05-29)` | 49 | 441 | `94e4f1b46b03c7ac89241ac98b3cba0d5712aaf5d4a236970aa6212bffd71fa1` |
| FINAL_HOLDOUT | `[2026-05-29, 2026-07-17)` | 49 | 441 | `560480a951b57ef481e1238948ae6277f12c8c1d3eda3c2822b2ce983a6c928f` |

All 1,773 daily memberships and 9,927 fragment references resolve structurally: 4,212 backfill and 5,715 prospective references, representing 8,244 unique planned fragment paths. Accepted real plans reached only `PLAN_VERIFIED`.

## Production orchestrator

The repository-native entry point is:

```text
ak-engine pr4b0-r1-epoch <operation> --config <canonical-config> --epoch-root <isolated-root>
```

Production Go code calls the typed `github.com/david22573/ak-rif/research` authority, P7 stage-execution-set APIs, Engine qualification runner, partition materializer, and canonical receipt/result sealers directly. It exposes all required explicit lifecycle operations plus read-only `status` and durable `resume`. Completed receipt-bearing work cannot replay; a retry requires durable proof of zero rows, zero outcomes, and zero result artifacts; indeterminate access blocks both authorities.

The exact production runner identity is source commit `6075e42c31df456c9faefdae03f1f4cd9695672f`, package SHA-256 `547dbee820b222706a0d24e57b1539c6dbe63aa350eac7e82f55f83db9b0cfa8`, and deterministic binary SHA-256 `7cd16826c01cab401bab965399b75682e07f39b635840f8eb87a7c4d6b306f89`. The independently verified accepted P7 binary is `371153d611cebce5838caebf1a57346f15f566c7494042538758b133cd98faa7`.

Exact P7 schemas are `ak.rif.stage_execution_set.v1`, `ak.rif.research_governance_store.v2`, `ak.rif.research_governance_envelope.v2`, and `ak.rif.execution_result_envelope.v1`.

## V00 universe resolution

Interpretation A is proven from source and contracts: BTCUSDT is a required context dependency, not a scored V00 target.

- V00 source: `sha256:3c2e20fd5bf615864aebc5be35ce86c15a6ed8f83de33b2f1d33b00dae6fbfa1`
- V00 configuration: `sha256:9a3b4d2797daedac643491b8b420b033d05ca46bf051f9fa42b656eb29ede4de`
- Universe contract: `sha256:aa74974d23f39b031e4832490720a5d739d61ce9301e41d276490eff8e0fa35e`
- Dataset: ADA, AVAX, BNB, BTC, DOGE, ETH, LINK, SOL, XRP (`USDT` suffixes)
- Targets: ADA, AVAX, BNB, DOGE, ETH, LINK, SOL, XRP (`USDT` suffixes)
- Context only: BTCUSDT

Candidate semantics did not change. Candidate events, qualification gates, and concentration retain accepted primary-target population semantics; BTC remains complete and available in the artifact but cannot be scored as a target.

The accepted gate set (`647e5d…2635`), independence (`84a686…c1f`), uncertainty (`1a9154…8fb`), and concentration (`a12684…39d5`) identities are unchanged.

## Drills and real structural preflight

The production-shaped synthetic drill exercised synthetic checkpoint construction, all three plan/materialization lifecycles, four-variant DEVELOPMENT, deterministic nominee selection, nominee-plus-two-neighbor VALIDATION, candidate freeze, one-shot FINAL_HOLDOUT, legitimate terminal closeout, crash/resume, replay rejection, zero-access retry, and indeterminate-access blocking through production code.

The accepted-checkpoint preflight at semantic commit `6075e42c…` returned `READY_TO_REGISTER_PROTOCOL`. Its canonical config is `sha256:21a5a059036e68edb3e8d798e3c0f2a9a9d6ab7bb44ef0f9ce922cb8b6ab2cd3`. The epoch root contains only orchestrator state, a registry, and three canonical plans. All three registry entries are `PLAN_VERIFIED`; next operation is `register-protocol`.

Real protocol registrations: 0. Real partition access count: 0. Real candidate outcome count: 0. No protocol, RIF store, authorization, access receipt, artifact, candidate event, outcome, or result exists in the accepted preflight root.

An independently running prospective Historian daemon added later unreferenced nine-symbol cycles and updated some source atimes during the audit window. Those external daemon changes are outside the content-addressed accepted plan. The P8 preflight code only verifies/registers plans and contains no materialize/consume call; it opened no fragment bytes.

## Verification

All required commands passed:

```text
# Engine
GOWORK=off go test ./... -count=1
GOWORK=off go vet ./...
GOWORK=off go test -race ./internal/qualificationrunner ./internal/rifbridge ./internal/app -count=1

# RIF
GOWORK=off go test ./... -count=1
GOWORK=off go vet ./...
GOWORK=off go test -race ./research -count=1

# unchanged Historian
GOWORK=off go test ./... -count=1
GOWORK=off go vet ./...
```

Two deterministic production builds matched byte-for-byte; a separate CLI build reproduced `sha256:7cd16826…6f89` and its `pr4b0-r1-epoch --help` invocation passed. Changed first-party Go files pass gofmt and diff checks. Exact ancestry/diff, dependency, secret, trader/order API, checkpoint mutation, no-real-access, no-outcome, and clean-worktree audits passed. Repository-wide gofmt reports only pre-existing first-party and third-party vendor formatting outside the changed P8 first-party files.

The canonical JSON companion records the exact semantic file lists, all semantic commits, complete identities, and exact command outcomes.
